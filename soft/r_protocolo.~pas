unit r_protocolo;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ComCtrls, DB, DBTables, DBCtrls, Grids, DBGrids,
  ExtCtrls ;

type
  Tfrmospt = class(TForm)
    GroupBox1: TGroupBox;
    BtOK: TButton;
    Button3: TButton;
    GroupBox4: TGroupBox;
    CbFiscal: TComboBox;
    procedure FormActivate(Sender: TObject);
    procedure FormClose(Sender: TObject; var Action: TCloseAction);
    procedure Button3Click(Sender: TObject);
    procedure BtOKClick(Sender: TObject);
    procedure CbFiscalChange(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  frmospt: Tfrmospt;

implementation

uses relretorno, login, relarequerimento, relaoficio, relptpen;

{$R *.dfm}

procedure Tfrmospt.FormActivate(Sender: TObject);
begin
// tbos.open;
cbfiscal.Text:=frmlogin.c_user;
if (frmlogin.c_grp = 'ADM') or (frmlogin.c_grp = 'FIS') or (frmlogin.c_grp = 'CAD') then
begin
    cbfiscal.enabled:=true;
    cbfiscal.setfocus;
end
else cbfiscal.enabled:=false;
if (frmlogin.c_grp = 'ADM') or (frmlogin.c_grp = 'FIS') or (frmlogin.c_grp = 'CAD') then BtOk.Enabled:=true else
  begin
      BtOk.Enabled:= false;
      cbfiscal.Text:= '';
  end;
end;

procedure Tfrmospt.FormClose(Sender: TObject; var Action: TCloseAction);
begin
// tbreq.Close;
end;

procedure Tfrmospt.Button3Click(Sender: TObject);
begin
close;
end;

procedure Tfrmospt.BtOKClick(Sender: TObject);
begin

{if dtpfim.date > Date then
begin
   showmessage('Data Final maior que atual');
   exit;
end;

if dtpini.date > Date then
begin
   showmessage('Data Inicial maior que atual');
   exit;
end;


if dtpfim.date < dtpini.Date then
begin
   showmessage('Data Final menor que a inicial');
   exit;
end;  }
         rel_ptpen.tbprotocolo.open;
         rel_ptpen.tbcontrib.open;
         rel_ptpen.tbtramita.open;
         rel_ptpen.QRLFiscal.Caption      := cbfiscal.Text;
         rel_ptpen.tbprotocolo.Filter     := 'Carga ='+ quotedstr(cbfiscal.Text);
         rel_ptpen.repptpen.PreviewModal;
         rel_ptpen.tbtramita.open;
         rel_ptpen.tbcontrib.close;
         rel_ptpen.tbprotocolo.close;
end;

procedure Tfrmospt.CbFiscalChange(Sender: TObject);
begin
 perform(Wm_nextdlgctl,0,0);
end;

end.
